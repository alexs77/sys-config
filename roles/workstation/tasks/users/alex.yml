- name: users | alex | remove preinstalled clutter from home directory
  ansible.builtin.file:
    path: /home/alex/{{ item }}
    state: absent
  with_items:
    - examples.desktop
    - Desktop
    - Documents
    - Downloads
    - Music
    - Pictures
    - Public
    - Templates
    - Videos

- name: users | alex | create personal config directories
  tags: dotfiles,dotfiles-alex
  ansible.builtin.file:
    path: /home/alex/{{ item.dir }}
    state: directory
    owner: alex
    group: alex
    mode: 0700
  with_items:
    - { dir: .config }
    - { dir: .config/autostart }
    - { dir: .config/bpytop }
    - { dir: .config/gtk-3.0 }
    - { dir: .config/terminator }
    - { dir: .local }
    - { dir: .local/share }
    - { dir: .local/share/applications }
    - { dir: bin }
    - { dir: desktop }
    - { dir: documents }
    - { dir: downloads }
    - { dir: music }
    - { dir: pictures }
    - { dir: projects }
    - { dir: templates }
    - { dir: videos }

- name: users | alex | copy user-dirs.dirs
  tags: dotfiles,dotfiles-alex
  ansible.builtin.copy:
    src: users/alex/user-dirs.dirs
    dest: /home/alex/.config/user-dirs.dirs
    owner: alex
    group: alex
    mode: 0600
  notify: update_xdg

- name: users | alex | copy personal config files
  tags: dotfiles,dotfiles-alex
  ansible.builtin.copy:
    src: users/alex/{{ item.src }}
    dest: /home/alex/{{ item.dest }}
    owner: alex
    group: alex
    mode: 0600
  with_items:
    - { src: 'asunder', dest: '.asunder' }
    - { src: 'bpytop.conf', dest: '.config/bpytop/bpytop.conf' }
    - { src: 'directory_bookmarks', dest: '.config/gtk-3.0/bookmarks' }
    - { src: 'gvimrc', dest: '.gvimrc' }
    - { src: 'linode-cli', dest: '.config/linode-cli' }
    - { src: 'mimeapps.list', dest: '.config/mimeapps.list' }
    - { src: 'ssh_client_config', dest: '.ssh/config' }
    - { src: 'terminator_config', dest: '.config/terminator/config' }

- name: users | alex | copy profile image
  tags: dotfiles,dotfiles-alex
  ansible.builtin.copy:
    src: users/alex/profile_image.png
    dest: /var/lib/AccountsService/icons/alex.png
    owner: root
    group: root
    mode: 0644
